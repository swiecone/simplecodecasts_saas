
<div class="row ">
   <div class="col-md-6 col-md-offset-3 grey well">
     <% if params[:plan] == '2' %>
    
   <h1 class="text-center ">Pro Account
    </h1>
    <p class="text-center"> Sign up for free and get basic membership account.</p>
  </div>
    <% else %>
    <h1 class="text-center">Basic Account</h1><p class="text-center"> Sign up for Premium account for 10$/monthand get community information </p>
  </div>
    <% end %>
  </div>
</div>

  <div class="col-md-6 col-md-offset-3">
    <div class="well">
      <h2>Sign up</h2>
          <% if params[:plan] == '1' %>
              <%= render "basic_form" %>
          <% else %>
            <%= render "premium_form" %>
          <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

